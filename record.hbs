<!DOCTYPE html>
<html>

{{> head }}

<body class="cBallerina-io">
    {{> bioBody }}
    <div class="row cBallerina-io-Gray-row">
        <div class="container">
            <div class="row">
                <div class="col-xs-12 col-sm-12 col-md-3 col-lg-3 cBallerina-io-Home-Middle-col">

                    <nav class="cLeftNavContainer">
                        <ul class="cMainLeftNav">
                            <li class="sub-menu cTopicURL OpenUL">
                                <div class="cLeftMenuLink cTopiAtag">Records</div>
                                <ul class="sub-ul Open-submenu" style="display: none;">
                                    {{#each module.records}}
                                    <li {{#equals record.name name}}class="active" {{/equals}}>
                                        <a href="{{name}}.html">{{name}}</a>
                                    </li>
                                    {{/each}}
                                </ul>
                            </li>
                            {{> currentModule }}
                        </ul>
                    </nav>
                </div>


                <div class="col-xs-12 col-sm-12 col-md-9 col-lg-9 cBallerina-io-Home-Middle-col">
                    <div id="search-impl" class="search-bar"></div>

                    <div id="main" class="cBlallerina-io-docs-content-container">
                        <div class="wy-nav-content">
                            <div class="rst-content">
                                <div role="main">
                                    {{> breadcrumbhead }}
                                    <li>/</li>
                                    <li><a href="{{ rootPath }}{{module.id}}/index.html">{{module.id}}</a></li>
                                    <li>/</li>
                                    <li><a href="{{ rootPath }}{{module.id}}/index.html#records">Records</a></li>
                                    <li>/</li>{{ record.name }}
                                    {{> breadcrumbfoot }}

                                    <div class="section">
                                        <h1>
                                            <span class="title-type">Record - </span>
                                            {{#if project.isSingleFile}}
                                            {{project.sourceFileName}}
                                            {{else}}
                                            <a href="{{ rootPath }}{{module.id}}/index.html">{{module.id}}</a>
                                            {{/if}}
                                            : <span
                                                class="records {{#if record.isDeprecated}}strike{{/if}}">{{ record.name }}</span>
                                        </h1>
                                        <p>
                                            {{#if record.isDeprecated}}
                                            <span class="stab">Deprecated</span>
                                            {{/if}}
                                            {{{ record.description }}}
                                        </p>
                                        <div class="constants">
                                            {{#if record.fields}}
                                            {{#record}}
                                            <h2>Fields</h2>
                                            {{> fields }}
                                            {{/record}}
                                            {{/if}}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{> foot }}
</body>

{{> footer }}